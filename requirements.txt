# Core dependencies
# (kein Flask nötig - wir nutzen stdlib http.server)

# Playwright für Weizen-Scraping (finanzen.net)
playwright>=1.40.0

# Google Gemini Vision für Screenshot-Analyse
google-generativeai>=0.3.0
Pillow>=10.0.0
